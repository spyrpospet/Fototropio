%input{class: "tab", name: "tab_locale", type: "radio", role: "tab", "aria-label":t("images")}
%div{class:"tab-content bg-base-100 border-base-300 rounded-box p-6", role: "tabpanel"}
  %div{class: "flex justify-start gap-x-3 mb-6"}
    %div{class: "flex flex-col gap-y-3", "data-controller": "preview"}
      - if @page.sub_image.present?
        = image_tag(@page.sub_image, "data-preview-target": "preview")
      - else
        = image_tag("https://placehold.co/300x300", size: "300x300", "data-preview-target": "preview")
      %div{class: "flex flex-row py-3 gap-x-3 border-t border-b justify-center"}
        %div #{t("dimensions")}:
        %div 400x400
      = f.file_field :sub_image, direct_upload: true, "data-preview-target": "source", "data-action": "change->preview#update"
  %div{class: "flex justify-start gap-x-3"}
    %div{class: "flex flex-col gap-y-3", "data-controller": "preview"}
      - if @page.image.present?
        = image_tag(@page.image, "data-preview-target": "preview")
      - else
        = image_tag("https://placehold.co/300x300", size: "300x300", "data-preview-target": "preview")
      %div{class: "flex flex-row py-3 gap-x-3 border-t border-b justify-center"}
        %div #{t("dimensions")}:
        %div 532x716
      = f.file_field :image, direct_upload: true, "data-preview-target": "source", "data-action": "change->preview#update"

    %div{class: "flex flex-col gap-y-3", "data-controller": "preview"}
      - if @page.second_image.present?
        = image_tag(@page.second_image, "data-preview-target": "preview")
      - else
        = image_tag("https://placehold.co/300x300", size: "300x300", "data-preview-target": "preview")
      %div{class: "flex flex-row py-3 gap-x-3 border-t border-b justify-center"}
        %div #{t("dimensions")}:
        %div 298x387
      = f.file_field :second_image, direct_upload: true, "data-preview-target": "source", "data-action": "change->preview#update"