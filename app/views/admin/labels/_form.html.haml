= content_for :body do
  = simple_form_for [:admin, @label], html: { class: "flex flex-col" } do |f|
    %div{class: "flex flex-row items-center justify-between border-b pb-3 mb-6", id: "label-heading"}
      %h1{class: "text-2xl font-bold"} #{action_name == "new" ? t("new_label") : t("edit_label")}
      %div{class: "flex flex-row items-center gap-x-3"}
        = link_to t("cancel"), admin_labels_path, class: "btn btn-neutral"
        = f.submit t("save"), class: "btn btn-neutral"
    %div{class: "flex flex-col test"}
      %div{id: "page-errors"}
      %div{class: "tabs tabs-lifted", role: "tablist"}
        = render partial: 'details', locals: { f: f, locale: locale }